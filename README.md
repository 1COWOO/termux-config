# termux-config
그냥 내가 쓰려고 만든 termux 세팅 파일임
## 🚀 Quick Start (한 줄 설치)

아래 명령어를 복사해서 Termux에 붙여넣으세요:

```bash
pkg install curl -y && curl -LO https://raw.githubusercontent.com/1COWOO/termux-config/main/install.sh && chmod +x install.sh && ./install.sh
